{erl_opts, [debug_info]}.
{deps, []}.

{relx, [{release, {ewallet, "0.1.0"},
         [ewallet]},

        {dev_mode, true},
        {include_erts, false},

        {extended_start_script, true}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [ewallet]}
]}.
